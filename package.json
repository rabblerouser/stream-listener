{
  "name": "@rabblerouser/event-forwarder",
  "version": "0.0.1",
  "description": "A lambda function that receives all events from a kinesis stream and forwards them to APIs over HTTP",
  "scripts": {
    "start": "nodemon -L localDev.js",
    "test": "NODE_ENV=test mocha -r src/__tests__/testGlobals.js --recursive src/__tests__"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/rabblerouser/event-forwarder.git"
  },
  "license": "AGPL-3.0",
  "dependencies": {
    "request": "^2.79.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "@rabblerouser/local-kinesis-lambda-runner": "^0.2.5",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "nodemon": "^1.15.0",
    "sinon": "^4.3.0",
    "sinon-chai": "^2.14.0"
  }
}
